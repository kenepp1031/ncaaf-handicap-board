# Repo stores text as LF; working trees on Windows may check out CRLF.
# Without this, every file written by an editor or generated by the pipeline
# shows up as a whole-file rewrite in the diff.
* text=auto
*.db binary
*.ico binary
